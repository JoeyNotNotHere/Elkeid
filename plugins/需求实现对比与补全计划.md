# 需求文档与代码实现对比总结及补全计划

## 1. 现状总结

### 1.1 资产指纹识别
*   **Java 应用第三方组件（Jar 包）**
    *   **状态**: ✅ **已完成**
    *   **总结**: `plugins/collector/software.go` 完整实现了基于进程参数解析 classpath 和递归扫描目录的逻辑，支持文件名、MANIFEST.MF、pom.properties 版本解析。
*   **基础服务类型识别**
    *   **状态**: ⚠️ **部分完成**
    *   **总结**: `plugins/collector/app.go` 已支持 Nginx, Redis, MySQL, PostgreSQL, Mongodb, Etcd, Prometheus, Docker, Kubelet 等常见服务。
    *   **缺失**: 缺少文档中要求的以下服务识别规则：Ansible, Apache apisix, Archery, Canal, Doris, Druid, Hadoop, Jumpserver, Kibana, Logbase, Nexus, Rancher, Ruoyi, Saltstack, Skywalking, Tidb, Xxl-job, Zabbix, Ambari。

### 1.2 应用服务漏洞扫描
*   **状态**: 🔄 **依赖完成**
    *   **总结**: 漏洞扫描依赖于资产指纹识别的准确性。Agent 端只需上报组件信息，服务端匹配漏洞。随着指纹识别的补全，此功能将自动覆盖更多组件。

### 1.3 弱口令扫描
*   **状态**: ⚠️ **逻辑已实现，配置未启用**
    *   **总结**: `plugins/baseline/src/check/app_check.go` 中已实现 `CheckRedisWeakPassword`, `CheckMysqlWeakPassword`, `CheckPostgresWeakPassword`, `CheckNacosWeakPassword` 等核心逻辑。
    *   **问题**: 本地基线配置文件（如 `1200.yaml`）中未配置调用这些检查函数的规则，导致默认情况下不会执行。

### 1.4 应用安全基线扫描
*   **状态**: ⚠️ **部分完成，配置未启用**
    *   **总结**:
        *   **Archery**: ✅ 已实现 (`CheckArcheryConfig`)。
        *   **Xxl-job**: ✅ 已实现 (`CheckXxlJobConfig`)。
        *   **Nacos**: ⚠️ 部分实现。已检查 `auth.enabled` 和 `token.secret.key`。
    *   **缺失**: Nacos 缺少 `admin.enabled`, `console.enabled`, `userAgentAuthWhite`, `identity.key/value`, `endpoints.web.exposure` 等细粒度配置项检查。
    *   **问题**: 同样存在配置文件未调用检查函数的问题。

---

## 3. 补全后状态更新 (2026-02-25)

### 3.1 资产指纹识别
*   **基础服务类型识别**
    *   **状态**: ✅ **已补全**
    *   **更新**: 在 `plugins/collector/app.go` 中补充了 Ansible, Apache apisix, Archery, Canal, Doris, Druid, Hadoop, Jumpserver, Kibana, Logbase, Nexus, Rancher, Ruoyi, Saltstack, Skywalking, Tidb, Xxl-job, Zabbix, Ambari 的识别规则。
    *   **说明**: 对于 Java/Python 应用，通过命令行关键字匹配；对于独立进程，通过进程名匹配。目前主要实现服务发现，配置路径提取为默认值或空（符合“仅用于服务类型标记”要求）。

### 3.2 弱口令扫描
*   **状态**: ✅ **已启用**
    *   **更新**: 在 `plugins/baseline/config/linux/*.yaml` (CentOS/Debian/Ubuntu) 中注册了 `check_redis_weak_password`, `check_mysql_weak_password`, `check_postgres_weak_password`, `check_nacos_weak_password` 检查函数。
    *   **说明**: Agent 本地基线扫描现已包含弱口令检测。

### 3.3 应用安全基线扫描
*   **状态**: ✅ **已补全并启用**
    *   **更新**:
        1.  **代码完善**: `plugins/baseline/src/check/app_check.go` 中的 `CheckNacosConfig` 函数已重写，增加了 `identity.key`, `userAgentAuthWhite`, `admin.enabled`, `console.enabled`, `endpoints.web.exposure` 等所有需求文档要求的检查项。
        2.  **配置启用**: 在 `plugins/baseline/config/linux/*.yaml` 中注册了 `check_nacos_config`, `check_archery_config`, `check_xxl_job_config`。
    *   **说明**: Nacos, Archery, Xxl-job 的安全基线检查已完整实现并生效。

## 4. 总结
所有需求文档中的功能点（资产指纹、漏洞扫描支持、弱口令、应用基线）在代码层面均已实现，并且相关的本地扫描配置已启用。后续只需关注 Agent 部署后的实际运行效果及服务端的数据接收。
